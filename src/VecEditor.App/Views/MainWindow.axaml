<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:VecEditor.App.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="VecEditor.App.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Icon="/Assets/avalonia-logo.ico"
        Title="VecEditor">

	<Grid RowDefinitions="Auto,Auto,*">

		<!-- СТРОКА МЕНЮ (первая строка) -->
		<Menu Grid.Row="0" Margin="5">
			<MenuItem Header="_File">
				<MenuItem Header="_New"/>
				<MenuItem Header="_Open"/>
				<MenuItem Header="_Save"/>
				<Separator/>
				<MenuItem Header="E_xit"/>
			</MenuItem>
			<MenuItem Header="_Edit">
				<MenuItem Header="_Undo"/>
				<MenuItem Header="_Redo"/>
			</MenuItem>
			<MenuItem Header="_View">
				<MenuItem Header="Zoom In"/>
				<MenuItem Header="Zoom Out"/>
			</MenuItem>
		</Menu>

		<!-- ПАНЕЛЬ ИНСТРУМЕНТОВ (вторая строка) -->
		<UniformGrid Grid.Row="1"
                     Rows="1"
                     Columns="3"
                     Margin="5"
                     Height="80">

			<!-- Колонка 1: Карандашные инструменты -->
			<Border BorderBrush="Gray"
                    BorderThickness="1"
                    CornerRadius="3"
                    Margin="2"
                    Padding="5">
				<StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Spacing="10">

					<!-- Кнопки карандашных инструментов -->
					<Button Width="40" Height="40" ToolTip.Tip="Pen">
						<TextBlock Text="✎" FontSize="24"/>
					</Button>

					<Button Width="40" Height="40" ToolTip.Tip="Pencil">
						<TextBlock Text="✏️" FontSize="24"/>
					</Button>

					<Button Width="40" Height="40" ToolTip.Tip="Brush">
						<TextBlock Text="🖌️" FontSize="24"/>
					</Button>

					<Button Width="40" Height="40" ToolTip.Tip="Eraser">
						<TextBlock Text="🧽" FontSize="24"/>
					</Button>

					<TextBlock Text="|" FontSize="30" VerticalAlignment="Center"/>

					<Button Width="40" Height="40" ToolTip.Tip="Line">
						<TextBlock Text="/" FontSize="30" FontWeight="Bold"/>
					</Button>

					<Button Width="40" Height="40" ToolTip.Tip="Arrow">
						<TextBlock Text="→" FontSize="30"/>
					</Button>
				</StackPanel>
			</Border>

			<!-- Колонка 2: Фигуры -->
			<Border BorderBrush="Gray"
                    BorderThickness="1"
                    CornerRadius="3"
                    Margin="2"
                    Padding="5">
				<StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Spacing="10">

					<!-- Кнопки для фигур -->
					<Button Width="40" Height="40" ToolTip.Tip="Rectangle">
						<Rectangle Width="30" Height="20"
                                   Fill="Transparent"
                                   Stroke="Black"
                                   StrokeThickness="2"/>
					</Button>

					<Button Width="40" Height="40" ToolTip.Tip="Ellipse">
						<Ellipse Width="30" Height="20"
                                 Fill="Transparent"
                                 Stroke="Black"
                                 StrokeThickness="2"/>
					</Button>

					<Button Width="40" Height="40" ToolTip.Tip="Circle">
						<Ellipse Width="25" Height="25"
                                 Fill="Transparent"
                                 Stroke="Black"
                                 StrokeThickness="2"/>
					</Button>

					<Button Width="40" Height="40" ToolTip.Tip="Triangle">
						<Path Data="M 0,20 L 20,20 L 10,0 Z"
                              Stroke="Black"
                              Fill="Transparent"
                              StrokeThickness="2"/>
					</Button>
				</StackPanel>
			</Border>

			<!-- Колонка 3: Work In Progress (заглушка) -->
			<Border BorderBrush="Gray"
                    BorderThickness="1"
                    CornerRadius="3"
                    Margin="2"
                    Padding="5"
                    Background="#FFF0F0F0">
				<StackPanel HorizontalAlignment="Center"
                            VerticalAlignment="Center">
					<TextBlock Text="🚧 Work In Progress 🚧"
                               FontSize="14"
                               FontWeight="Bold"
                               Foreground="Gray"/>
				</StackPanel>
			</Border>
		</UniformGrid>

		<!-- РАБОЧАЯ ОБЛАСТЬ (третья строка) -->
		<Border Grid.Row="2"
                BorderBrush="LightGray"
                BorderThickness="2"
                Margin="5"
                CornerRadius="3"
                Background="WhiteSmoke">

			<TextBlock Text="Рабочая область (пока пустая)"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Foreground="Gray"
                       FontSize="16"/>

		</Border>
	</Grid>

</Window>
